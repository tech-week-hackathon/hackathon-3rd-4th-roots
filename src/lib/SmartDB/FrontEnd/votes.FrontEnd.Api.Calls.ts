import { BaseFrontEndApiCalls    } from 'smart-db';
import { votesEntity } from '../Entities/votes.Entity';

export class votesApi extends  BaseFrontEndApiCalls    {
    protected static _Entity = votesEntity;

    // #region api

    // #endregion api
}
