import { BaseFrontEndApiCalls    } from 'smart-db';
import { VoteEntity } from '../Entities/Vote.Entity';

export class VoteApi extends  BaseFrontEndApiCalls    {
    protected static _Entity = VoteEntity;

    // #region api

    // #endregion api
}
