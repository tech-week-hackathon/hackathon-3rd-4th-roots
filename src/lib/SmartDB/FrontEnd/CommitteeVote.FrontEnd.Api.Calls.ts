import { BaseFrontEndApiCalls    } from 'smart-db';
import { CommitteeVoteEntity } from '../Entities/CommitteeVote.Entity';

export class CommitteeVoteApi extends  BaseFrontEndApiCalls    {
    protected static _Entity = CommitteeVoteEntity;

    // #region api

    // #endregion api
}
