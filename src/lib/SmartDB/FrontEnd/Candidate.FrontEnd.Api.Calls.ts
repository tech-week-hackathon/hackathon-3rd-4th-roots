import { BaseFrontEndApiCalls    } from 'smart-db';
import { CandidateEntity } from '../Entities/Candidate.Entity';

export class CandidateApi extends  BaseFrontEndApiCalls    {
    protected static _Entity = CandidateEntity;

    // #region api

    // #endregion api
}
