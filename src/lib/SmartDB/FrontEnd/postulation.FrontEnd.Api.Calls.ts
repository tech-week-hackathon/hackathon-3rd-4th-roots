import { BaseFrontEndApiCalls    } from 'smart-db';
import { postulationEntity } from '../Entities/postulation.Entity';

export class postulationApi extends  BaseFrontEndApiCalls    {
    protected static _Entity = postulationEntity;

    // #region api

    // #endregion api
}
