import { BaseFrontEndApiCalls    } from 'smart-db';
import { ConstitutionEntity } from '../Entities/Constitution.Entity';

export class ConstitutionApi extends  BaseFrontEndApiCalls    {
    protected static _Entity = ConstitutionEntity;

    // #region api

    // #endregion api
}
