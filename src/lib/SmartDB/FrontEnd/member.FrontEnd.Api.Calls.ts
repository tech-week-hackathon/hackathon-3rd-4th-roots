import { BaseFrontEndApiCalls    } from 'smart-db';
import { memberEntity } from '../Entities/member.Entity';

export class memberApi extends  BaseFrontEndApiCalls    {
    protected static _Entity = memberEntity;

    // #region api

    // #endregion api
}
